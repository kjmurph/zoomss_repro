% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zoomss_fishing.R
\name{setFishingParams}
\alias{setFishingParams}
\title{Configure catchability and selectivity parameters for effort-driven fishing}
\usage{
setFishingParams(
  Groups,
  q_small = 0,
  q_med = 0,
  q_large = 0,
  w_min_small = NULL,
  w_min_med = NULL,
  w_min_large = NULL
)
}
\arguments{
\item{Groups}{Data frame of functional groups (from getGroups())}

\item{q_small}{Numeric. Catchability for Fish_Small group (default: 0)}

\item{q_med}{Numeric. Catchability for Fish_Med group (default: 0)}

\item{q_large}{Numeric. Catchability for Fish_Large group (default: 0)}

\item{w_min_small}{Numeric. Log10 minimum weight (g) for Fish_Small selectivity.
If NULL (default), uses existing Fmort_W0 value.}

\item{w_min_med}{Numeric. Log10 minimum weight (g) for Fish_Med selectivity.
If NULL (default), uses existing Fmort_W0 value.}

\item{w_min_large}{Numeric. Log10 minimum weight (g) for Fish_Large selectivity.
If NULL (default), uses existing Fmort_W0 value.}
}
\value{
Modified Groups data frame with q column added/updated and optionally
updated Fmort_W0 values
}
\description{
Adds catchability coefficient (q) to the Groups data frame and optionally
updates selectivity bounds (Fmort_W0, Fmort_Wmax). This function prepares the Groups
data for use with effort time series in zoomss_model().
}
\details{
Set Fishing Parameters for Effort-Driven Fishing

When effort time series are provided in input_params, fishing mortality is
calculated dynamically at each time step as:

F(w, t) = Effort(t) * q * Selectivity(w)

where Selectivity is a knife-edge function: 1 for w in \link{Fmort_W0, Fmort_Wmax}, 0 otherwise.

The catchability coefficient q controls the efficiency of fishing effort at removing
biomass. It is the primary parameter to be estimated during calibration against
observed catch data.
}
\examples{
\dontrun{
Groups <- getGroups()
Groups <- setFishingParams(Groups, q_small = 0.01, q_med = 0.02, q_large = 0.03)

# With custom selectivity thresholds
Groups <- setFishingParams(Groups,
                           q_small = 0.01, q_med = 0.02, q_large = 0.03,
                           w_min_small = 0.5, w_min_med = 1.5, w_min_large = 3.0)
}

}
